# Sirra Framework Requirements
# Optimized for iOS (a-Shell/Pythonista), Android (Termux), Linux, and Windows

# Core dependencies
requests>=2.31.0
aiohttp>=3.9.1
colorama>=0.4.6

# Optional but recommended for advanced scanning
dnspython>=2.4.2
